// The always-on maximum distance a volunteer will ever see an event/pin at,
// on both Map and Find — not adjustable via any filter UI. Shared so
// map-pins.ts and find-filters.ts enforce the exact same ceiling from one
// source of truth.
export const MAX_EVENT_DISTANCE_MILES = 25;
