// `verified` defaults to true so the verification badge is visible for the
// signed-in user out of the box — there's no "how to get verified" flow yet,
// so this is a demo default rather than an earned state.
export const CURRENT_USER = {
  id: 'me',
  name: 'You',
  verified: true,
};
